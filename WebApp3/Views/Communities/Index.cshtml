@model IEnumerable<WebApp3.Models.Community>

@{
    ViewData["Title"] = "Travel blog";
}

<h1>Travel Blog</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<hr/>


<div class="row mt-5 mb-2 ml-3 mr-3 align-content-center ">
    @{
        foreach (var item in Model)
        {
            @*@Html.DisplayFor(modelItem => item.Pic)*@

    <div class="card" style="width:350px">
        @if (item.UserName == User.Identity.Name)
        {
            <img class="card-img-top" asp-route-id="@item.id" src="~/Content/Images/@item.Pic" asp-append-version="true" alt="KAKOVA blin....... NE GRUZITSA AAAAAAAAA">
            <div class="card-body">
                <h4 class="card-title"> @Html.DisplayFor(modelItem => item.Name)</h4>
                <p class="card-text">   @Html.DisplayFor(modelItem => item.Description)</p>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a>
                <a asp-action="Details" asp-route-id="@item.id">Details</a>
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </div>
        }

        else if (User.Identity.Name == "tanya3@routes.com")
        {
            <img class="card-img-top" asp-route-id="@item.id" src="~/Content/Images/@item.Pic" asp-append-version="true" alt="KAKOVA blin....... NE GRUZITSA AAAAAAAAA">
            <div class="card-body">
                <h4 class="card-title"> @Html.DisplayFor(modelItem => item.Name)</h4>
                <p class="card-text">   @Html.DisplayFor(modelItem => item.Description)</p>
                <a asp-action="Edit" asp-route-id="@item.id">Edit</a>
                <a asp-action="Details" asp-route-id="@item.id">Details</a>
                <a asp-action="Delete" asp-route-id="@item.id">Delete</a>
            </div>
        }

        else if (item.UserName != User.Identity.Name)
        {
            <img class="card-img-top" asp-route-id="@item.id" src="~/Content/Images/@item.Pic" alt="KAKOVA blin............. NE GRUZITSA AAAAAAAAA">
            <div class="card-body">
                <h4 class="card-title"> @Html.DisplayFor(modelItem => item.Name)</h4>
                <p class="card-text">   @Html.DisplayFor(modelItem => item.Description)</p>

                <a asp-action="Details" asp-route-id="@item.id">Details</a>
            </div>
        }
    </div>
        

        }



    }
</div>
         